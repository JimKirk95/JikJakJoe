using UnityEngine;
using UnityEngine.SceneManagement;


public class TTTIntroJMF : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        SceneManager.LoadScene(1);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
